dataset:
  data_path: /kaggle/input/datasets/ltttuyen/flickr8k/flickr8k
  latents_path: /kaggle/input/datasets/ltttuyen/flickr8k-latents-train/Flickr8k_latents_train
  text_embeds_path: /kaggle/input/datasets/ltttuyen/flickr8k-embeddings-train/Flickr8k_embeddings_train
  image_size: 256
  num_workers: 4
  p_uncond: 0.1

model:
  name: DiT-S/2
  vae: ema

training:
  epochs: 1500
  global_batch_size: 128
  global_seed: 0
  optimizer:
    lr: 1e-4
    weight_decay: 0

sampling:
  model_type: model
  cfg_scale: 1.0
  num_sampling_steps: 1000
  prompt: A man riding a bicycle.

logging:
  results_dir: results
  log_every: 10
  ckpt_every: 10
  resume_ckpt: /kaggle/input/datasets/ltttuyen/checkpoint-0033000/checkpoint_0033000.pt
  is_save_to_s3: true